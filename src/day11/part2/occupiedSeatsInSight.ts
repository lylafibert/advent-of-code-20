export default (
  seats: string[],
  rowIndex: number,
  seatIndex: number
): number => {
  let occupiedSeats = 0;
  for (let i = rowIndex - 1; i > 0; i--) {
    console.log("HERE");
  }
  return 3;
};
